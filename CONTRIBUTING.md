# Contributing

## Local development

You'll need Rust installed.

### Run

1. `cargo run`
2. <https://localhost:3000/stats>

### Run but with debug logs enabled

(recommended.)

- `RUST_LOG=debug cargo run`
